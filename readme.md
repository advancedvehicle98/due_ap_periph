# Зависимости
Ставим через pip всё, что есть в requirements.txt:
```bash
$ pip install -r requirements.txt
```

# Сборка
```bash
make

make compile # без генерации файлов для DroneCAN

make upload # чтобы залить прошивку
```
